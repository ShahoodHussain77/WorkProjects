import React, { Component } from 'react'
import { StatusBar } from 'react-native'

export class Statusbar extends Component {
    render() {
        return (
            <StatusBar backgroundColor="#414963" barStyle="light-content"/>
        )
    }
}

export default Statusbar